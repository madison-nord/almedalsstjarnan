# Features Directory

This directory is reserved for future feature modules.

Each feature module should be self-contained with its own types, logic, and tests. Features are imported via the `#features/*` path alias.

No feature modules have been implemented yet.
